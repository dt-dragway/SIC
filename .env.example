# 🔐 Variables de Entorno - SIC Ultra
# COPIA ESTE ARCHIVO COMO .env Y COMPLETA LOS VALORES
# NUNCA subir .env al repositorio (agregado a .gitignore)

# === APP ===
APP_NAME=SIC Ultra
APP_ENV=development
DEBUG=true

# === SECRETS (GENERA CON: python3 backend/generate_secrets.py) ===
SECRET_KEY=CAMBIAR_CON_VALOR_GENERADO_64_CARACTERES_MINIMO
JWT_SECRET_KEY=CAMBIAR_CON_VALOR_GENERADO_64_CARACTERES_MINIMO

# === DATABASE (NUNCA usar las mismas credenciales en dev y prod) ===
POSTGRES_USER=sic_user
POSTGRES_PASSWORD=CAMBIAR_CON_PASSWORD_SEGURA_AQUI
POSTGRES_DB=sic_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# === REDIS ===
REDIS_URL=redis://localhost:6379/0

# === BINANCE ===
# Obtén en: https://www.binance.com/en/my/settings/api-management
BINANCE_API_KEY=tu_api_key_aqui
BINANCE_API_SECRET=tu_api_secret_aqui
BINANCE_TESTNET=true

# === AUTH ===
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=30
REMEMBER_ME_EXPIRE_DAYS=30
TRUSTED_DEVICE_EXPIRE_DAYS=30

# === ADMIN (Opcional: Password para seed_data.py) ===
ADMIN_PASSWORD=genera_uno_aleatorio_con_script

# === 2FA ===
TOTP_ISSUER=SIC Ultra
